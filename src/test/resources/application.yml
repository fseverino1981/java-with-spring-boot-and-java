server:
  port: 8888
cors:
  originsPatterns: http://localhost:3000,http://localhost:8080,https://flavio.com.br
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
    open-in-view: false
springdoc:
  pathsToMatch: /**/v1/**
  swagger-ui:
    user-root-path: true